pipeline {
    agent {
        docker { image 'node:18.16.0-alpine' }
    }
    stages {
        stage('Test node docker') {
            steps {
                echo '*********************'
                sh 'node --version'
            }
        }
    }
}