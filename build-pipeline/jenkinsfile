pipeline {
    agent { label 'docker' }
    stages {
        stage('Test') {
            agent {
                docker { 
                    image 'node:18.16.0-alpine'
                    args '-u 0:0'
                }
            }
            steps {
                sh 'node --version'
            }
        }
    }
}