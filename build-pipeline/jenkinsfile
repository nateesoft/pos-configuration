pipeline {
    environment {
        app_name = "${app_name}"
    }
    agent any
    stages {
        stage('Sync git') {
            steps {
                echo "app_name = ${app_name}"
                build(job: 'My-pipeline')
            }
        }
    }    
}