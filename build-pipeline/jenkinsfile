pipeline {
    environment {
        app_name = "${app_name}"
    }
    options {
        ansiColor('xterm')
    }
    agent any
    stages {
        stage('Sync git') {
            steps {
                echo "app_name = ${app_name}"
            }
        }
    }    
}